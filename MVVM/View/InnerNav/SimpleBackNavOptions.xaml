<UserControl x:Class="Scene_Maker.MVVM.View.SimpleBackNavOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:Scene_Maker.MVVM.ViewModel"
             xmlns:local="clr-namespace:Scene_Maker.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Border Background="#3A315C"
            CornerRadius="10,10,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Margin="0,0,8,0"
            >
      
            
            <Button 
            HorizontalAlignment="Left"
            VerticalAlignment="Bottom"
            Grid.Row="0"
            Width="100"
            Style="{StaticResource InnerBackButtonTheme}"
            Content="← Back" 
            Command="{Binding BackCommand}"/>

        </Border>

    </Grid>
</UserControl>
